<div class="post">
  <h1> All Posts! </h1>
  <ul>
    <% @posts.each do |post| %>
      <li><%= post.title %>
        <%= post.author %>
        <%= post.body %>
        <a href="/posts/<%= post.id %>">show</a>
      </li>
    <% end %>
  </ul>
</div>
