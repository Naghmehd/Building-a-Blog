<h1> All Posts! </h1>

<ul>
  <% @posts.each do |post| %>
    <li><%= post.title %> </br>
      <%= post.author %> </br>
      <%= post.body %> </br>
      <a href="/posts/<%= post.id %>">show</a>
    </li>
  <% end %>
</ul>
